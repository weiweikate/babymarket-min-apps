<template name="cell-add-input">
  <view class="cell-add-input">
    <text class="input-title">{{title}}</text>
    <input class="input-value" name="{{key}}" value="{{value}}" placeholder="{{hint}}" placeholder-style="color:#dbdbdb" type="{{type}}" />
  </view>
</template>