<template name="tool-cell">
    <view class="tool-cell-view" catchtap="toolCellTap" data-title="{{title}}">
        <image src="{{imageUrl}}" class="image"></image>
        <label class="title">{{title}}</label>
    </view>
</template>